# ---------------------------------------------------------------------------
# SomaiyaSat Ground Control -- copy this file to .env and fill in your values.
# ---------------------------------------------------------------------------

# Superuser connection, used only by scripts/setup_db.py
PGHOST=localhost
PGPORT=5432
PGDATABASE=space_deploy
PGUSER=postgres
PGPASSWORD=change_me

# Login passwords for the three application roles created by 05_roles.sql.
# setup_db.py substitutes these into the script; the Streamlit login screen
# uses them to connect AS that role.
GROUND_OPERATOR_PASSWORD=ground123
STUDENT_ANALYST_PASSWORD=student123
AI_ROUTER_PASSWORD=router123
